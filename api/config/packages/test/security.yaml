security:
    providers:
        in_memory:
            memory:
                users:
                    testuser:  { password: test, roles: 'ROLE_CREW' }
                    admin: { password: kitten, roles: 'ROLE_ADMIN' }
