// import { put, takeEvery } from 'redux-saga/effects'
// import { initToken } from './actions'

// function* initializeApp(action) {
//   yield put(initToken(action.payload.csrfToken))
// }

// // use them in parallel
// export default function* artefactsSagas() {
//   yield takeEvery('APP_START', initializeApp)
// }